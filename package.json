{
  "name": "ticket-tracker-monorepo",
  "version": "1.0.0",
  "description": "Full-stack ticket tracker application with a React frontend and Node.js backend.",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.js",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:fast": "node scripts/start.js",
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "build:backend": "npm install --prefix backend",
    "start:prod": "npm start --prefix backend",
    "clean": "rm -rf node_modules backend/node_modules frontend/node_modules dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
